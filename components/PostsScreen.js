import React from "react";
import { View, Text } from "react-native";

export default function PostsScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Створити публікацію. step 3</Text>
    </View>
  );
}
